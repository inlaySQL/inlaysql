[package]
name = "inlaysql-mcp"
description = "MCP server mode for InlaySQL: hand a database file to an LLM agent as a tool."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "inlaysql"
path = "src/bin/inlaysql.rs"

[dependencies]
inlaysql = { workspace = true }
inlaysql-server = { workspace = true }
serde_json = { workspace = true }
