# Present so this directory can be dropped into an `ann-benchmarks` checkout as
# `ann_benchmarks/algorithms/inlaysql/` unchanged. The class lives in
# `module.py`, which is where `ann_benchmarks/definitions.py` looks for it.
